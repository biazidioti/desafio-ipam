const INITIAL_STATE = {
    estado: '',
    municipio: '',
};

const localidade = (
 state = INITIAL_STATE,
 action,
) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default localidade;