const INITIAL_STATE = {
    estado: '',
    municipio: '',
};

const states = (
 state = INITIAL_STATE,
 action,
) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default states;