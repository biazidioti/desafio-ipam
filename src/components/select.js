import { Component } from "react";

class Select extends Component {
    render() {
        return(
            <div>
                <select>Estado</select>
            </div>
        )
    }
}

export default Select;